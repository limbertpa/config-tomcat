# email
mail:
  domain: gmail.com
# info logging demo
#info:
  #id: ${spring.application.name}
#logging:
  #level:
    #org.springframework.security: DEBUG
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic
